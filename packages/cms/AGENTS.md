- Use Bun instead of NPM and PNPM. Use `bunx` instead of `npx`.

## Design System

- no font-bold, font-medium, font-semibold
- no border radii / no rounded corners
- very few shadows, only if elevation really makes sense
- primary color: #C2F653
