{
  "name": "@gqlweekly/e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run --cwd ../api migrate:up && CI=true playwright test",
    "test:dev": "bun run --cwd ../api migrate:up && playwright test",
    "test:ui": "bun run --cwd ../api migrate:up && playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "@types/bun": "^1.3.6"
  }
}
